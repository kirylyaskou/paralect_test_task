---
phase: 05-enhancements-and-deployment
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - README.md
  - .env.example
autonomous: false
requirements: [DEPL-01, DEPL-02, DEPL-03]

must_haves:
  truths:
    - "Application is deployed to Vercel and accessible via a public URL"
    - "README.md contains setup instructions with all required environment variables"
    - "README.md contains an architecture diagram showing the 3-layer separation"
    - "README.md contains a deploy link to the live application"
    - "README.md contains a placeholder for the Loom demo video link"
  artifacts:
    - path: "README.md"
      provides: "Project documentation with setup, architecture, and deployment info"
      contains: "Architecture"
  key_links:
    - from: "README.md"
      to: "Vercel deployment"
      via: "Deploy link URL"
      pattern: "vercel.app"
---

<objective>
Deploy the application to Vercel and create comprehensive README documentation with setup instructions, architecture diagram, and demo video placeholder.

Purpose: This is the final plan that makes the application publicly accessible and provides documentation for evaluators. The Loom demo video (DEPL-03) is recorded by the user separately -- the README includes a placeholder link.

Output: Live Vercel deployment, README.md with full documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-enhancements-and-deployment/05-CONTEXT.md
@.planning/phases/05-enhancements-and-deployment/05-RESEARCH.md
@.planning/phases/05-enhancements-and-deployment/05-01-SUMMARY.md
@.planning/phases/05-enhancements-and-deployment/05-02-SUMMARY.md
@.planning/phases/05-enhancements-and-deployment/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with setup instructions, architecture diagram, and deployment info</name>
  <files>
    README.md,
    .env.example
  </files>
  <read_first>
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md,
    package.json,
    .env.example,
    app/layout.tsx,
    lib/db/client.ts
  </read_first>
  <action>
Create `README.md` at project root (per D-16) with the following sections:

1. **Project Title and Description:**
   - "# AI Chatbot"
   - Brief description: A ChatGPT-like chatbot built with Next.js 16, Supabase, and OpenRouter. Features streaming AI responses, file attachments (images + documents), anonymous trial access, and multi-tab synchronization.

2. **Live Demo:**
   - "## Live Demo"
   - Deploy link: `[Live Application](https://YOUR_VERCEL_URL)` -- placeholder to be updated after deployment
   - Loom video: `[Demo Video (Loom)](https://www.loom.com/share/YOUR_VIDEO_ID)` -- placeholder for user to update (per D-17)

3. **Features:**
   - Bullet list of key features:
     - Real-time AI chat with token-by-token streaming
     - Image attachments with AI vision analysis (clipboard paste + upload)
     - PDF/DOCX document upload with text extraction as AI context
     - Anonymous trial (3 free questions without signup)
     - Multi-tab chat synchronization via Supabase Realtime
     - Dark/light theme toggle
     - Responsive design (desktop + mobile)

4. **Tech Stack:**
   - Table format: Next.js 16 (App Router), TypeScript, Tailwind CSS 4, shadcn/ui v4, Supabase (PostgreSQL, Storage, Realtime), AI SDK v6, OpenRouter (LLM provider), TanStack Query v5

5. **Architecture:**
   - "## Architecture"
   - Paragraph explaining the strict 3-layer separation: DB layer (`lib/db/`) -> API layer (`app/api/`) -> Client layer (components/hooks). Zero database calls from components. Supabase service_role key isolated server-side.
   - Mermaid diagram showing: Client Layer (React Components -> TanStack Query Hooks -> fetch to API Routes) -> API Layer (Next.js API Routes -> Auth + Zod Validation) -> DB Layer (lib/db/ Access Functions -> Supabase PostgreSQL + Storage). External: OpenRouter LLM API from API layer, Supabase Realtime to Client layer.

6. **Getting Started / Setup Instructions:**
   - "## Getting Started"
   - Prerequisites: Node.js 18+, pnpm
   - Clone: `git clone <repo-url> && cd <project>`
   - Install: `pnpm install`
   - Environment variables: List ALL required env vars with descriptions:
     - `SUPABASE_URL` -- Supabase project URL (from Supabase dashboard -> Settings -> API)
     - `SUPABASE_SERVICE_ROLE_KEY` -- Supabase service role key (from same location, keep secret)
     - `NEXT_PUBLIC_SUPABASE_URL` -- Same as SUPABASE_URL (public, used for Realtime/Storage)
     - `NEXT_PUBLIC_SUPABASE_ANON_KEY` -- Supabase anon key (from dashboard)
     - `OPENROUTER_API_KEY` -- OpenRouter API key (from openrouter.ai/keys)
     - `JWT_SECRET` -- Secret for JWT session tokens (generate with `openssl rand -base64 32`)
   - Copy `.env.example` to `.env.local` and fill in values
   - Supabase setup: Run migrations, create storage bucket, enable Realtime on chats table
   - Run dev: `pnpm dev`
   - Build: `pnpm build`

7. **Deployment:**
   - "## Deployment"
   - Push to GitHub, connect repo in Vercel dashboard
   - Configure environment variables in Vercel dashboard
   - Deploy triggers automatically on push to main

8. **Project Structure:**
   - Brief tree showing the key directories: app/, components/, hooks/, lib/db/, lib/ai/, lib/auth/

9. **Update `.env.example`** to include all required env vars if not already present:
   ```
   SUPABASE_URL=
   SUPABASE_SERVICE_ROLE_KEY=
   NEXT_PUBLIC_SUPABASE_URL=
   NEXT_PUBLIC_SUPABASE_ANON_KEY=
   OPENROUTER_API_KEY=
   JWT_SECRET=
   ```
  </action>
  <verify>
    <automated>test -f README.md && grep -q "Architecture" README.md && grep -q "SUPABASE_URL" README.md && grep -q "Getting Started" README.md && grep -q "mermaid" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root
    - README.md contains "## Architecture" section
    - README.md contains "mermaid" diagram block
    - README.md contains "## Getting Started" section
    - README.md contains all 6 env var names: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, OPENROUTER_API_KEY, JWT_SECRET
    - README.md contains "Loom" or "Demo Video" placeholder
    - README.md contains "pnpm install" and "pnpm dev" commands
    - .env.example contains all 6 env var names
  </acceptance_criteria>
  <done>README.md has project description, live demo link placeholder, features list, tech stack, architecture diagram (Mermaid), setup instructions with all env vars, deployment guide, and Loom video placeholder.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy to Vercel and record Loom demo video</name>
  <files>README.md</files>
  <action>
    Human deploys to Vercel and records Loom demo video. Claude updates README.md with the actual URLs afterward.

    **Vercel Deployment (DEPL-01):**
    1. Push all code to GitHub: `git push origin main`
    2. Go to https://vercel.com/new and import the GitHub repository
    3. Configure environment variables in Vercel dashboard:
       - SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, OPENROUTER_API_KEY, JWT_SECRET
    4. Click Deploy
    5. Note the deployment URL (e.g., https://your-app.vercel.app)

    **Supabase Configuration for Production:**
    1. In Supabase Dashboard -> SQL Editor, run:
       `INSERT INTO storage.buckets (id, name, public) VALUES ('attachments', 'attachments', true);`
       `ALTER PUBLICATION supabase_realtime ADD TABLE chats;`
       (Skip if already done during development)

    **Loom Demo Video (DEPL-03):**
    1. Record a 2-5 minute Loom video demonstrating all features
    2. Share the Loom URL

    After human provides URLs, Claude updates README.md replacing YOUR_VERCEL_URL and YOUR_VIDEO_ID with actual values.
  </action>
  <verify>
    <automated>! grep -q "YOUR_VERCEL_URL" README.md && ! grep -q "YOUR_VIDEO_ID" README.md && echo "PASS" || echo "Placeholders still present"</automated>
  </verify>
  <acceptance_criteria>
    - README.md no longer contains "YOUR_VERCEL_URL" -- replaced with actual Vercel deployment URL
    - README.md no longer contains "YOUR_VIDEO_ID" -- replaced with actual Loom video ID (or user confirms video will be added later)
    - Vercel deployment URL returns HTTP 200
  </acceptance_criteria>
  <done>Application deployed to Vercel with public URL. README updated with live deployment link and Loom demo video link.</done>
</task>

</tasks>

<verification>
1. `test -f README.md` -- README exists
2. `grep "Architecture" README.md` -- has architecture section
3. `grep "mermaid" README.md` -- has diagram
4. `grep "Getting Started" README.md` -- has setup instructions
5. Vercel URL is accessible (manual check after deployment)
</verification>

<success_criteria>
- README.md exists with setup instructions, architecture diagram, and deployment info
- Application deployed to Vercel and accessible via public URL
- README contains live demo link and Loom video link (or placeholders if user hasn't deployed/recorded yet)
- .env.example lists all required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/05-enhancements-and-deployment/05-04-SUMMARY.md`
</output>
