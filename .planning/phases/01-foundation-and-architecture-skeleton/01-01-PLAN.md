---
phase: 01-foundation-and-architecture-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - .prettierrc
  - .gitignore
  - .env.example
  - components.json
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - app/providers.tsx
  - lib/errors.ts
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "Project builds with Next.js 15 App Router, TypeScript strict mode, and Tailwind CSS 4"
    - "Shadcn/ui is initialized with OKLCH color variables in globals.css"
    - "TanStack Query provider wraps the application with correct singleton pattern"
    - "next-themes ThemeProvider is configured for dark/light mode"
    - "ESLint flat config + Prettier are configured and lint passes"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies"
      contains: "@supabase/supabase-js"
    - path: "app/providers.tsx"
      provides: "Client-side provider tree"
      contains: "QueryClientProvider"
    - path: "app/layout.tsx"
      provides: "Root layout with Inter font and Providers"
      contains: "Providers"
    - path: "app/globals.css"
      provides: "Tailwind v4 theme with OKLCH variables"
      contains: "@theme"
    - path: "eslint.config.mjs"
      provides: "ESLint flat config with Prettier"
      contains: "eslintConfigPrettier"
    - path: "lib/errors.ts"
      provides: "DatabaseError class for DB layer"
      exports: ["DatabaseError"]
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
  key_links:
    - from: "app/layout.tsx"
      to: "app/providers.tsx"
      via: "import and JSX wrapping"
      pattern: "import.*Providers.*from.*providers"
    - from: "app/providers.tsx"
      to: "@tanstack/react-query"
      via: "QueryClientProvider"
      pattern: "QueryClientProvider"
---

<objective>
Scaffold the Next.js 15 project with all dependencies, Shadcn/ui initialization, ESLint/Prettier configuration, TanStack Query + next-themes providers, root layout, and placeholder page.

Purpose: Create the runnable project foundation that all subsequent plans and phases build on. Without this, no code can be written or tested.
Output: A buildable, lintable Next.js 15 project with the full dependency tree installed and the provider/layout shell configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-architecture-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-and-architecture-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-and-architecture-skeleton/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project, install all dependencies, configure ESLint + Prettier</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, .prettierrc, .gitignore, .env.example</files>
  <read_first>
    - .planning/phases/01-foundation-and-architecture-skeleton/01-RESEARCH.md (installation commands, version numbers, ESLint flat config example, Prettier config)
    - .planning/phases/01-foundation-and-architecture-skeleton/01-CONTEXT.md (locked decisions: pnpm, ESLint+Prettier, path aliases, folder structure)
  </read_first>
  <action>
1. Scaffold the project using create-next-app (the current directory is the project root):
   ```bash
   pnpm create next-app@latest . --ts --tailwind --eslint --app --no-src-dir --import-alias "@/*"
   ```
   This creates: package.json, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, app/globals.css, app/layout.tsx, app/page.tsx, and installs Next.js 15, React 19, TypeScript, Tailwind CSS 4, ESLint.

   IMPORTANT: The `--no-src-dir` flag is critical -- CONTEXT.md specifies lib/db/, app/api/, components/, hooks/ at root level, not inside src/.

2. Initialize Shadcn/ui IMMEDIATELY after create-next-app (before any manual CSS changes):
   ```bash
   pnpm dlx shadcn@latest init --defaults
   ```
   This creates components.json, installs tw-animate-css and lucide-react, and scaffolds OKLCH theme variables in globals.css. Accept defaults: New York style, neutral base color, OKLCH color variables.

3. Install core dependencies:
   ```bash
   pnpm add @supabase/supabase-js @tanstack/react-query zod server-only next-themes
   ```

4. Install dev dependencies:
   ```bash
   pnpm add -D @tanstack/react-query-devtools prettier eslint-config-prettier supabase
   ```

5. Update eslint.config.mjs to include Prettier config (replaces scaffolded content):
   ```javascript
   import { defineConfig, globalIgnores } from 'eslint/config'
   import nextVitals from 'eslint-config-next/core-web-vitals'
   import eslintConfigPrettier from 'eslint-config-prettier/flat'

   const eslintConfig = defineConfig([
     ...nextVitals,
     eslintConfigPrettier,
     globalIgnores(['.next/**', 'out/**', 'build/**', 'next-env.d.ts']),
   ])

   export default eslintConfig
   ```

6. Create .prettierrc at project root:
   ```json
   {
     "semi": false,
     "singleQuote": true,
     "trailingComma": "es5",
     "tabWidth": 2,
     "printWidth": 100
   }
   ```

7. Create .env.example at project root (template for required env vars, NO real secrets):
   ```bash
   # Supabase (server-only -- NO NEXT_PUBLIC_ prefix for service_role)
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

   # OpenAI (used in Phase 4, template set up now)
   OPENAI_API_KEY=your-openai-api-key

   # Supabase public (for Realtime only -- Phase 5)
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

8. Ensure .gitignore includes (append if not present):
   ```
   .env.local
   .env*.local
   supabase/.temp/
   ```

9. Create the project directory structure (empty directories to establish conventions):
   ```bash
   mkdir -p lib/db lib/schemas lib/types components/ui components/chat components/sidebar hooks supabase/migrations
   ```

10. Verify: run `pnpm build` -- must succeed. Run `pnpm lint` -- must pass.
  </action>
  <verify>
    <automated>pnpm build && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@supabase/supabase-js"` in dependencies
    - package.json contains `"@tanstack/react-query"` in dependencies
    - package.json contains `"zod"` in dependencies
    - package.json contains `"server-only"` in dependencies
    - package.json contains `"next-themes"` in dependencies
    - package.json contains `"prettier"` in devDependencies
    - package.json contains `"eslint-config-prettier"` in devDependencies
    - package.json contains `"supabase"` in devDependencies
    - eslint.config.mjs contains `eslintConfigPrettier`
    - .prettierrc contains `"semi": false`
    - .env.example contains `SUPABASE_URL=`
    - .env.example contains `SUPABASE_SERVICE_ROLE_KEY=`
    - .env.example contains `OPENAI_API_KEY=`
    - .gitignore contains `.env.local`
    - .gitignore contains `supabase/.temp/`
    - components.json exists (shadcn initialized)
    - tsconfig.json contains `"@/*"` path alias
    - Directory lib/db/ exists
    - Directory lib/schemas/ exists
    - Directory lib/types/ exists
    - Directory supabase/migrations/ exists
    - `pnpm build` exits with code 0
    - `pnpm lint` exits with code 0
  </acceptance_criteria>
  <done>Project scaffolded with all dependencies installed, ESLint+Prettier configured, Shadcn/ui initialized, directory structure created, and build+lint passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create providers, root layout, placeholder page, and DatabaseError class</name>
  <files>app/providers.tsx, app/layout.tsx, app/page.tsx, app/globals.css, lib/errors.ts</files>
  <read_first>
    - app/providers.tsx (if exists from scaffolding -- see current state before overwriting)
    - app/layout.tsx (see scaffolded layout to understand what create-next-app generated)
    - app/page.tsx (see scaffolded page)
    - app/globals.css (see shadcn-generated theme variables -- MUST preserve OKLCH theme)
    - .planning/phases/01-foundation-and-architecture-skeleton/01-RESEARCH.md (Pattern 3: DatabaseError, Pattern 4: TanStack Query Provider, Root Layout example)
    - .planning/phases/01-foundation-and-architecture-skeleton/01-UI-SPEC.md (typography: Inter font, metadata copy, placeholder page copy)
  </read_first>
  <action>
1. Create app/providers.tsx -- the client-side provider tree using TanStack Query v5 singleton pattern + next-themes:
   ```typescript
   'use client'

   import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
   import { ReactQueryDevtools } from '@tanstack/react-query-devtools'
   import { ThemeProvider } from 'next-themes'
   import { type ReactNode } from 'react'

   function makeQueryClient() {
     return new QueryClient({
       defaultOptions: {
         queries: {
           staleTime: 60 * 1000,
           gcTime: 5 * 60 * 1000,
           retry: 1,
           refetchOnWindowFocus: false,
         },
       },
     })
   }

   let browserQueryClient: QueryClient | undefined

   function getQueryClient() {
     if (typeof window === 'undefined') {
       return makeQueryClient()
     }
     if (!browserQueryClient) {
       browserQueryClient = makeQueryClient()
     }
     return browserQueryClient
   }

   export function Providers({ children }: { children: ReactNode }) {
     const queryClient = getQueryClient()

     return (
       <QueryClientProvider client={queryClient}>
         <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
           {children}
         </ThemeProvider>
         <ReactQueryDevtools initialIsOpen={false} />
       </QueryClientProvider>
     )
   }
   ```

   CRITICAL: Do NOT use `useState(() => new QueryClient())` -- this causes hydration issues with App Router suspense boundaries. Use the getQueryClient() singleton pattern per TanStack v5 docs.

2. Update app/layout.tsx -- root layout with Inter font, metadata, Providers wrapper:
   ```typescript
   import type { Metadata } from 'next'
   import { Inter } from 'next/font/google'
   import './globals.css'
   import { Providers } from './providers'

   const inter = Inter({ subsets: ['latin'] })

   export const metadata: Metadata = {
     title: 'Chatbot',
     description: 'AI Chatbot - Paralect Product Academy',
   }

   export default function RootLayout({
     children,
   }: {
     children: React.ReactNode
   }) {
     return (
       <html lang="en" suppressHydrationWarning>
         <body className={inter.className}>
           <Providers>{children}</Providers>
         </body>
       </html>
     )
   }
   ```

   IMPORTANT: `suppressHydrationWarning` on `<html>` is required by next-themes to prevent hydration mismatch from the class attribute injection. The metadata title is "Chatbot" and description is "AI Chatbot - Paralect Product Academy" per UI-SPEC.md copywriting contract.

3. Update app/page.tsx -- minimal placeholder page confirming the app works:
   ```typescript
   export default function Home() {
     return (
       <main className="flex min-h-screen flex-col items-center justify-center p-8">
         <h1 className="text-xl font-semibold leading-tight">Chatbot</h1>
         <p className="mt-4 text-base text-muted-foreground">Application is loading...</p>
       </main>
     )
   }
   ```

   Per UI-SPEC.md: heading = "Chatbot", body = "Application is loading...", using text-xl font-semibold for heading (Heading role) and text-base for body (Body role).

4. PRESERVE app/globals.css -- do NOT overwrite the Shadcn-generated OKLCH theme variables. Only verify it contains the `@theme inline` block and Tailwind v4 imports. If the file already has the correct content from shadcn init, leave it as-is. The file should have:
   - `@import "tailwindcss";` (or similar Tailwind v4 import)
   - `@theme inline { ... }` block with OKLCH color variables
   - Light and dark mode variable definitions
   If any of these are missing, something went wrong with shadcn init.

5. Create lib/errors.ts -- the DatabaseError class used by all DB layer functions:
   ```typescript
   import type { PostgrestError } from '@supabase/supabase-js'

   export class DatabaseError extends Error {
     public readonly code: string | null
     public readonly details: string | null

     constructor(message: string, pgError?: PostgrestError) {
       super(message)
       this.name = 'DatabaseError'
       this.code = pgError?.code ?? null
       this.details = pgError?.details ?? null
     }
   }
   ```

6. Verify: run `pnpm build` -- must succeed.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - app/providers.tsx contains `'use client'` as first line
    - app/providers.tsx contains `function getQueryClient()` (singleton pattern, NOT useState)
    - app/providers.tsx contains `QueryClientProvider`
    - app/providers.tsx contains `ThemeProvider`
    - app/providers.tsx contains `attribute="class"`
    - app/providers.tsx contains `defaultTheme="system"`
    - app/providers.tsx contains `staleTime: 60 * 1000`
    - app/providers.tsx contains `ReactQueryDevtools`
    - app/layout.tsx contains `import { Inter } from 'next/font/google'`
    - app/layout.tsx contains `import { Providers } from './providers'`
    - app/layout.tsx contains `title: 'Chatbot'`
    - app/layout.tsx contains `description: 'AI Chatbot - Paralect Product Academy'`
    - app/layout.tsx contains `suppressHydrationWarning`
    - app/layout.tsx contains `<Providers>{children}</Providers>`
    - app/page.tsx contains `Chatbot`
    - app/page.tsx contains `Application is loading...`
    - app/globals.css contains `@theme` (OKLCH theme block from shadcn)
    - lib/errors.ts contains `export class DatabaseError extends Error`
    - lib/errors.ts contains `PostgrestError`
    - lib/errors.ts contains `this.name = 'DatabaseError'`
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Providers component with TanStack Query singleton pattern + next-themes, root layout with Inter font and metadata, placeholder home page, and DatabaseError class all created. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds (TypeScript compiles, Next.js bundles)
- `pnpm lint` passes (ESLint flat config with Prettier)
- All expected directories exist: lib/db/, lib/schemas/, lib/types/, components/ui/, hooks/, supabase/migrations/
- components.json exists (Shadcn/ui initialized)
- app/globals.css contains OKLCH theme variables
</verification>

<success_criteria>
- Project builds and runs with Next.js 15 App Router, TypeScript strict mode, Tailwind CSS 4, and Shadcn/ui
- TanStack Query QueryClient is configured with singleton pattern (staleTime 60s, gcTime 5min, retry 1)
- next-themes ThemeProvider wraps the app with class attribute and system default
- ESLint flat config + Prettier configured and passing
- All core and dev dependencies installed
- .env.example documents all required environment variables
- DatabaseError class ready for DB layer consumption
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-architecture-skeleton/01-01-SUMMARY.md`
</output>
